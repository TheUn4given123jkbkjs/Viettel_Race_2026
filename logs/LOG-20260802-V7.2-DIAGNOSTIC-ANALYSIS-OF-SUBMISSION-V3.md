# 📌 LOG-20260802-V7.2: Phân tích nguyên nhân điểm thấp của lượt nộp V3 (Submission V3 Diagnostic)

* **Ngày thực hiện:** `2026-08-02 06:34:00 (UTC+7)`
* **Tác giả:** Antigravity (AI Coding Assistant) & User
* **Mục tiêu:** Phân tích nguyên nhân sâu xa khiến kết quả nộp bài V3 chỉ đạt **8.8679/100** điểm (với WER = 88.95%, J_assertion = 13.79%, J_candidates = 3.54%), dù đã gỡ OOM và tăng tốc độ suy luận thành công.

---

## 🔍 1. Kết quả Giám định Số liệu Tập Tin
Em đã viết và chạy script giám định [detailed_v3_diagnostic.py](file:///d:/AI%20Race/Viettel_Race_2026/scratch/detailed_v3_diagnostic.py) so sánh trực tiếp 100 tệp kết quả dự đoán với 100 tệp đầu vào gốc và tập dữ liệu huấn luyện, thu được các phát hiện chính sau:

1. **Sự chênh lệch về độ dài văn bản:**
   * Độ dài trung bình tập Train: **387.5 từ**.
   * Độ dài trung bình tập Test: **436.7 từ** (chứa nhiều tệp rất dài như `1.txt` với 5920 bytes, khoảng 900+ từ).
2. **Độ đậm đặc thực thể (Density):**
   * Tập Train: đạt trung bình **1.67 thực thể / 100 từ**.
   * Tập Test V3: chỉ đạt **1.46 thực thể / 100 từ**.
   * *Kết luận:* Số thực thể trích xuất thực tế bị sụt giảm lớn so với độ dài của tệp.

---

## 🛠️ 2. Bốn Nguyên Nhân Kỹ Thuật Khách Quan

### A. Lỗi Recall Thấp nghiêm trọng do Độ dài Văn bản & Vòng lặp Lặp Từ (Repetition Loop)
* **Chi tiết:** 
  * Qwen 2.5 7B được huấn luyện chỉ **1 epoch** (`trainer_state.json`) nên mang xu hướng cứng nhắc từ tập Train là chỉ sinh từ **5 - 8 thực thể** cho mỗi văn bản rồi đóng danh sách JSON.
  * Khi gặp tệp Test dài, mô hình dừng sinh sớm hoặc rơi vào vòng lặp lặp từ vô hạn (ví dụ cụm `"sàng lọc sớm các bệnh bẩm sinh"` bị lặp lại **4 lần** trong `1.json`), khiến các thực thể chẩn đoán/triệu chứng quan trọng khác ở cuối tệp bị bỏ sót hoàn toàn.
  * Tỷ lệ recall thực tế rơi xuống khoảng 10-15%, kéo WER tăng vọt lên **88.95%**.

### B. Bỏ qua các Từ bị Che/Ẩn (Masked Words - `************`)
* **Chi tiết:**
  * Tập Test chứa tổng cộng **99 từ bị ẩn** (sử dụng chuỗi `***` để che tên thuốc hoặc tên bệnh, ví dụ trong `100.txt` che tên thuốc Aspirin).
  * Trong V3, chỉ có **4 thực thể chứa '*'** được trích xuất. Việc bỏ qua các từ khóa bị che này làm mất hoàn toàn điểm Recall của các nhãn `THUỐC` tương ứng.

### C. Mã Candidates chứa Ký tự Đặc biệt (`*` hoặc `†`)
* **Chi tiết:**
  * Có 5 mã candidates trong V3 chứa dấu hoa thị như `"D63.0*"` lấy ra từ CSDL SQLite `medical_codes.db`.
  * Hệ thống chấm điểm tự động đối khớp chuỗi theo mã ICD-10 chuẩn hóa quốc tế sẽ không công nhận mã chứa ký tự đặc biệt này, kéo tụt điểm `J_candidates` xuống **3.54%**.

### D. Nhiễu nhãn y khoa (Entity Type Misclassification)
* **Chi tiết:**
  * Mô hình bị nhầm lẫn giữa nhãn giải phẫu và chẩn đoán. Ví dụ cụm `"động mạch vành"` bị gán nhãn `CHẨN_ĐOÁN` và map sang `"I25.4"` (Phình động mạch vành), dẫn đến bị phạt đúp điểm (sai type tính 0 điểm và mất thực thể chẩn đoán thực tế).

---

## 📈 3. Đề xuất Hành động cải tiến cho V5.0
1. **Chia nhỏ văn bản (Paragraph/Sliding Window Chunking):** Cắt văn bản test dài thành các phần nhỏ dưới 250 từ để trích xuất riêng biệt, tránh length prior bias và vòng lặp lặp từ của LLM, sau đó hợp nhất kết quả.
2. **Bổ sung hình phạt lặp từ (`repetition_penalty = 1.15`)** trong hàm `model.generate()`.
3. **Làm sạch mã candidates:** Tự động lọc sạch ký tự `*` hoặc `†` khỏi các mã candidates (ví dụ chuyển `D63.0*` thành `D63.0`) trước khi xuất file.
