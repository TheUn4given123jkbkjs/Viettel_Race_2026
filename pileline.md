 BẢN ĐỒ PHÂN CHIA TÁC VỤ TRONG PIPELINEPlaintext[Input Text] 
    │
    ├──► [TÁC VỤ 1: NER & POSITION] ─────────► (Dùng Chưng cất LLM + Lệnh cứng Python)
    │
    ├──► [TÁC VỤ 2: ASSERTION STATUS] ──────► (Dùng Chưng cất LLM)
    │
    └──► [TÁC VỤ 3: CANDIDATE LINKING] ──────► (Dùng Cache + RAG Vector / MedCAT)
1. TÁC VỤ 1: NER & VỊ TRÍ (text, type, position)Nhiệm vụ: Tìm đoạn từ y tế, phân loại 1 trong 5 nhãn (THUỐC, CHẨN_ĐOÁN, TRIỆU_CHỨNG, TÊN_XÉT_NGHIỆM, KẾT_QUẢ_XÉT_NGHIỆM) và tính vị trí [start, end].Chưng cất (Distillation) ở đâu?Chưng cất khả năng "Nhận diện & Cắt đoạn": Ép Teacher Model (GPT-4o) dạy Llama-3-8B cách trích xuất đoạn text chính xác và gán đúng nhãn type ngữ cảnh phức tạp (ví dụ: phân biệt đâu là tên xét nghiệm, đâu là kết quả xét nghiệm).Lệnh cứng (Code Python) ở đâu?Xác định position [start, end] tuyệt đối KHÔNG cho LLM đoán mò! LLM rất kém trong việc đếm chính xác từng ký tự (character index).Giải pháp lệnh cứng: LLM chỉ cần nhả ra chuỗi text (VD: "ho đờm xanh"). Bạn dùng 1 hàm Python ngắn (re.finditer hoặc String matching) quét lại trong văn bản gốc để lấy chính xác mảng [start, end].2. TÁC VỤ 2: SUY LUẬN BỐI CẢNH (assertions)Nhiệm vụ: Xác định xem CHẨN_ĐOÁN, THUỐC, TRIỆU_CHỨNG có bị Phủ định (isNegated), Tiền sử (isHistorical), hay thuộc Gia đình (isFamily) hay không.Chưng cất (Distillation) ở đâu?ĐÂY LÀ NƠI CẦN CHƯNG CẤT 100%! Suy luận bối cảnh tiếng Việt rất lắt léo (ví dụ: "Bệnh nhân không có biểu hiện ho nhưng người nhà có tiền sử...").Thuật toán luật cứng (như NegEx cũ trong nghiên cứu i2b2) sẽ bị sót rất nhiều bối cảnh phức tạp.Bạn cần chưng cất khả năng "Hiểu ngữ cảnh sâu" từ GPT-4o sang Llama-3-8B để Llama-3-8B tự động điền danh sách assertions: ["isNegated", ...] chuẩn xác.Lệnh cứng ở đâu?Viết filter kiểm tra: Nếu type là TÊN_XÉT_NGHIỆM hoặc KẾT_QUẢ_XÉT_NGHIỆM thì tự động ép mảng assertions = [] (vì đề bài quy định 2 loại này không tính assertions).3. TÁC VỤ 3: TÌM MÃ CHUẨN HOÁ (candidates: ICD-10 / RxNorm)Nhiệm vụ: Ánh xạ CHẨN_ĐOÁN $\rightarrow$ ICD-10 và THUỐC $\rightarrow$ RxNorm.Chưng cất ở đâu?KHÔNG CHƯNG CẤT HOÀN TOÀN TÁC VỤ NÀY VÀO TRỌNG SỐ LLM! Có hơn 10,000+ mã ICD-10 và hàng chục ngàn mã RxNorm, nếu ép Llama-3-8B học thuộc lòng mã, mô hình sẽ bị sinh mã ảo (Hallucination).Hệ thống RAG + Bộ Cache hoạt động thế nào?Bộ Cache (Lệnh cứng - Exact Match / Dictionary Lookup):Tạo 1 file JSON/Redis Cache lưu sẵn các cặp từ thông dụng đã chuẩn hóa (VD: "sốt cao" $\rightarrow$ ["R50.9"], "amlodipine 10mg" $\rightarrow$ ["308135"]).Nếu text trích ra trùng khớp 100% với Cache $\rightarrow$ Lấy ngay kết quả trong 0.001 giây mà không cần RAG hay LLM.Master Data RAG (Vector Search / MedCAT):Khi text không có trong Cache, dùng mô hình Vector Embedding (như bge-m3) bắn vào database ICD-10/RxNorm để lấy Top 5 candidates gần nhất.LLM Reranker (Chưng cất nhẹ - tùy chọn):Nếu Top 5 candidates của RAG trả về chưa chắc chắn, dùng Llama-3-8B đọc Top 5 đó kèm mô tả để chọn ra 1-3 mã phù hợp nhất.📊 TỔNG KẾT CHIẾN LƯỢC CHO BẠNThành phần Output JSONPhương pháp xử lý chínhKỹ thuật triển khai chi tiếttext & typeChưng cất (Distillation)Train Llama-3-8B trích xuất Concept & Phân loại nhãn.positionLệnh cứng (Python)Tìm vị trí index ký tự bằng String Searching sau khi có text.assertionsChưng cất (Distillation)Train Llama-3-8B suy luận ngữ cảnh (phủ định/tiền sử/gia đình).candidatesCache + RAG VectorCache Exact Match $\rightarrow$ Vector Search (BGE-M3) $\rightarrow$ Filter.